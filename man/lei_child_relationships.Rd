% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gleifr.R
\name{lei_child_relationships}
\alias{lei_child_relationships}
\title{Fetch the child relationship records of a LEI}
\usage{
lei_child_relationships(id, type = c("direct", "ultimate"), limit = 200L)
}
\arguments{
\item{id}{(\code{character(1)})\cr
The Legal Entity Identifier (LEI) to fetch the child relationships for.}

\item{type}{(\code{character(1)})\cr
The type of child relationships to fetch. One of \code{"direct"} or \code{"ultimate"}.
Default is \code{"direct"}.}

\item{limit}{(\code{NULL} | \code{integer(1)})\cr
Maximum number of records to return. Default \code{200L}. Use \code{NULL} to fetch all.}
}
\value{
A \code{data.frame()} with one row per relationship and the same columns as
\code{\link[=lei_parent_relationship]{lei_parent_relationship()}}, where \strong{start_node} is the child and \strong{end_node} is \code{id}.

When the LEI has no children, \code{NULL}.
}
\description{
Fetches the relationship records that link a LEI to its direct or ultimate children. Where
\code{\link[=lei_children]{lei_children()}} returns the children's own LEI records, this returns the relationships
themselves: their type, status, the periods they cover, and how they were corroborated.
}
\examples{
\donttest{
lei_child_relationships("O2RNE8IBXP4R0TD8PU41", limit = 5)

lei_child_relationships("O2RNE8IBXP4R0TD8PU41", type = "ultimate", limit = 5)
}
}
\seealso{
\code{\link[=lei_parent_relationship]{lei_parent_relationship()}} for the relationship to the parent of a LEI,
\code{\link[=lei_children]{lei_children()}} for the children's LEI records.
}
