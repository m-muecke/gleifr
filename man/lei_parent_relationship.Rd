% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gleifr.R
\name{lei_parent_relationship}
\alias{lei_parent_relationship}
\title{Fetch the parent relationship record of a LEI}
\usage{
lei_parent_relationship(id, type = c("direct", "ultimate"))
}
\arguments{
\item{id}{(\code{character(1)})\cr
The Legal Entity Identifier (LEI) to fetch the parent relationship for.}

\item{type}{(\code{character(1)})\cr
The type of parent relationship to fetch. One of \code{"direct"} or \code{"ultimate"}.
Default is \code{"direct"}.}
}
\value{
A \code{data.frame()} with one row and columns (all dates are \code{POSIXct} in UTC):
\itemize{
\item \strong{start_node}: The LEI of the child entity
\item \strong{end_node}: The LEI of the parent entity
\item \strong{relationship_type}: The relationship type, e.g. \code{"IS_DIRECTLY_CONSOLIDATED_BY"}
\item \strong{relationship_status}: The relationship status, e.g. \code{"ACTIVE"}
\item \strong{relationship_period_start}, \strong{relationship_period_end}: When the relationship holds
\item \strong{accounting_period_start}, \strong{accounting_period_end}: The accounting period it was reported for
\item \strong{document_filing_period_start}, \strong{document_filing_period_end}: The filing period of the
supporting document, or \code{NA} if none
\item \strong{initial_registration_date}: When the relationship was first registered
\item \strong{last_update_date}: When the relationship was last updated
\item \strong{registration_status}: The registration status, e.g. \code{"PUBLISHED"} or \code{"LAPSED"}
\item \strong{next_renewal_date}: When the relationship is due for renewal
\item \strong{managing_lou}: The LEI of the managing Local Operating Unit
\item \strong{corroboration_level}: How the relationship was validated, e.g. \code{"FULLY_CORROBORATED"}
\item \strong{corroboration_documents}: The type of supporting document, or \code{NA} if none
\item \strong{corroboration_reference}: A reference to the supporting document, or \code{NA} if none
\item \strong{valid_from}, \strong{valid_to}: When the record is valid in the GLEIF database
}

Errors when no parent relationship is reported for the LEI.
}
\description{
Fetches the relationship record that links a LEI to its direct or ultimate parent. Where
\code{\link[=lei_parent]{lei_parent()}} returns the parent's own LEI record, this returns the relationship itself: its type,
status, the periods it covers, and how it was corroborated.
}
\examples{
\donttest{
lei_parent_relationship("529900W18LQJJN6SJ336")

lei_parent_relationship("529900W18LQJJN6SJ336", type = "ultimate")
}
}
\seealso{
\code{\link[=lei_child_relationships]{lei_child_relationships()}} for the relationships to the children of a LEI,
\code{\link[=lei_parent]{lei_parent()}} for the parent's LEI record.
}
