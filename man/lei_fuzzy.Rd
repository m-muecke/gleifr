% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gleifr.R
\name{lei_fuzzy}
\alias{lei_fuzzy}
\title{Fuzzy search for LEI records}
\source{
\url{https://www.gleif.org/en/lei-data/gleif-api}
}
\usage{
lei_fuzzy(q, field = c("fulltext", "entity.legalName", "owns", "ownedBy"))
}
\arguments{
\item{q}{(\code{character(1)})\cr
The search query.}

\item{field}{(\code{character(1)})\cr
The field to search. One of \code{"fulltext"}, \code{"entity.legalName"}, \code{"owns"}, or \code{"ownedBy"}.
Default is \code{"fulltext"}.}
}
\value{
A \code{data.frame()} with columns:
\itemize{
\item \strong{value}: The matched value
\item \strong{lei}: The Legal Entity Identifier of the matched record, or \code{NA} if none is linked
}

When nothing matches, \code{NULL}.
}
\description{
Performs a typo-tolerant, approximate search against the GLEIF API. Unlike the filters in
\code{\link[=lei_records]{lei_records()}}, which match literal substrings, this matches on edit distance and returns
candidate values together with their LEI. Useful for resolving messy or misspelled entity names
to a LEI, which can then be passed to \code{\link[=lei_record_by_id]{lei_record_by_id()}}.
}
\examples{
\donttest{
lei_fuzzy("Deutsch Bank", field = "entity.legalName")
}
}
\seealso{
\code{\link[=lei_autocomplete]{lei_autocomplete()}} for prefix-based completion, \code{\link[=lei_record_by_id]{lei_record_by_id()}} for full records.
}
