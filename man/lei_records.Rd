% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gleifr.R
\name{lei_records}
\alias{lei_records}
\title{Fetch LEI records}
\usage{
lei_records(
  legal_name = NULL,
  fulltext = NULL,
  country = NULL,
  jurisdiction = NULL,
  registration_status = NULL,
  entity_status = NULL,
  category = NULL,
  isin = NULL,
  ...,
  limit = 200L,
  simplify = TRUE
)
}
\arguments{
\item{legal_name}{(\code{NULL} | \code{character(1)})\cr
Filter by legal name.}

\item{fulltext}{(\code{NULL} | \code{character(1)})\cr
Full-text search query.}

\item{country}{(\code{NULL} | \code{character(1)})\cr
Filter by legal address country (ISO 3166-1 alpha-2 code), e.g. \code{"DE"}.}

\item{jurisdiction}{(\code{NULL} | \code{character(1)})\cr
Filter by jurisdiction (ISO 3166-1 alpha-2 code), e.g. \code{"DE"}.}

\item{registration_status}{(\code{NULL} | \code{character(1)})\cr
Filter by LEI registration status, e.g. \code{"ISSUED"} or \code{"LAPSED"}.}

\item{entity_status}{(\code{NULL} | \code{character(1)})\cr
Filter by entity status, one of \code{"ACTIVE"} or \code{"INACTIVE"}.}

\item{category}{(\code{NULL} | \code{character(1)})\cr
Filter by entity category, e.g. \code{"FUND"}, \code{"BRANCH"}, or \code{"GENERAL"}.}

\item{isin}{(\code{NULL} | \code{character(1)})\cr
Filter by ISIN.}

\item{...}{Additional filter parameters passed to the GLEIF API.
These are appended as query parameters, e.g.
\code{"filter[entity.subCategory]" = "CENTRAL_GOVERNMENT"}.}

\item{limit}{(\code{NULL} | \code{integer(1)})\cr
Maximum number of records to return. Default \code{200L}. Use \code{NULL} to fetch all matching records.}

\item{simplify}{(\code{logical(1)})\cr
Should the output be simplified? Default \code{TRUE}.}
}
\value{
When \code{simplify = TRUE}, a long-format \code{data.frame()} with columns:
\itemize{
\item \strong{lei}: The Legal Entity Identifier
\item \strong{name}: The attribute name
\item \strong{value}: The attribute value
}

When \code{simplify = FALSE}, a \code{list()} of the raw record objects from the API.

When no records match, \code{NULL}.
}
\description{
Fetch LEI records from the GLEIF database, optionally filtered by attribute. The filters are
combined with logical AND, so each one you supply narrows the results further. To fetch a single
record by its LEI, see \code{\link[=lei_record_by_id]{lei_record_by_id()}}.
}
\examples{
\donttest{
# search by legal name
head(lei_records(legal_name = "Deutsche Bank", limit = 5))

# filter by country and registration status
head(lei_records(country = "DE", registration_status = "ISSUED", limit = 5))
}
}
\seealso{
\code{\link[=lei_record_by_id]{lei_record_by_id()}} to fetch a single record by its LEI.
}
